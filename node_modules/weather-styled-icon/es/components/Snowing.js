import React from 'react';
import WeatherContainer from './WeatherContainer';
import Flake from './Flake';

var Snowing = function Snowing() {
  return React.createElement(
    WeatherContainer,
    null,
    React.createElement(Flake, null),
    React.createElement(Flake, null)
  );
};

export default Snowing;
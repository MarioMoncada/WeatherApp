import React from 'react';
import WeatherContainer from './WeatherContainer';
import Drop from './Drop';

var Drops = function Drops() {
  return React.createElement(
    WeatherContainer,
    null,
    React.createElement(Drop, null)
  );
};

export default Drops;